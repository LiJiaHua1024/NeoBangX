# 任务

上面那条 assistant 消息是同一篇译文的前半部分，它在译到一半时被打断了。你的输出会拼在它后面，接着往下译完。

# 怎么接

- 从断点之后的第一个字开始：断在句中就接着这句话往下译，断在段中就接着这一段往下译。已经译出的部分不重译。
- 沿用上文已经确立的术语、译名与格式（标题层级、列表、表格的列与表头），与前面的译文连起来是一整篇。
- 只输出译文：不写「继续」「以下是剩余部分」这类交代，不复述上文，不加译者注或原文。

# 例外

如果原文已经全部译完、没有剩余内容，只输出这一行：

@@CONTINUE_DONE@@
