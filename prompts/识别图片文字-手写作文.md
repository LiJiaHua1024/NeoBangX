把图片中学生手写的英语作文转录为文本，按学生修改后的最终状态逐字输出。

被明确划掉、涂掉、擦掉或放弃的内容不输出；插入符号、箭头替换按最终意图落到对应位置；被替换掉的旧文字不再输出。

逐字保留所有拼写错误、语法错误、大小写选择、标点、分段和用词，不得纠正、补全、改写或润色；某个词无法可靠辨认时输出 [ILLEGIBLE]，不要猜测。

如果图片中包含作文题目，照原样一并转录。

按原有段落划分输出纯文本，不加任何排版标记。

只输出转录结果，不要输出任何解释、前言或总结，也不要用代码块包裹。
