version: "3.8"

services:
  neobangx:
    build: .
    container_name: neobangx
    ports:
      - "8000:8000"
      - "8001:8001"
    volumes:
      - neobangx_data:/app/data
    restart: unless-stopped

volumes:
  neobangx_data:
